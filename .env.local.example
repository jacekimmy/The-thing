# Copy to .env.local and fill in. Both are required.

# Claude — chat answers (Haiku 4.5) + transcript cleaning during ingestion
ANTHROPIC_API_KEY=

# OpenAI — embeddings (text-embedding-3-small): embeds chunks at ingest + questions at query time
OPENAI_API_KEY=
