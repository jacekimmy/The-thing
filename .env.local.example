# Copy to .env.local and fill in. Both are required.

# Claude — chat answers (Haiku 4.5) + transcript cleaning during ingestion
ANTHROPIC_API_KEY=

# Voyage — hosted embeddings (voyage-3.5-lite): embeds chunks at ingest +
# the question at query time. Free tier at https://voyageai.com
VOYAGE_API_KEY=
